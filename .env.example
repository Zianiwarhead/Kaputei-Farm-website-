# Copy to .env (never commit .env)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# The site works WITHOUT these (bundled local data is used as fallback).
# Add them when the Supabase project (see supabase/schema.sql) is ready.
